plugins() {
    id('java')
}

group = project.parent.group
version = project.parent.version

repositories() {
    mavenCentral()
}

dependencies() {
    compileOnly("org.jetbrains:annotations:${version_java_annotations}")

    testImplementation(platform("org.junit:junit-bom:${version_jupiter}"))
    testImplementation('org.junit.jupiter:junit-jupiter')
}

test() {
    useJUnitPlatform()
}